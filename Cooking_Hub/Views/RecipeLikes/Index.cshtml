@model IEnumerable<Cooking_Hub.Models.RecipeLike>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LikedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Recipe)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LikedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Recipe.RecipeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RecipeLikeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RecipeLikeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RecipeLikeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
